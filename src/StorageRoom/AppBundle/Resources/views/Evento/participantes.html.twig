<div class="well col-sm-10 col-sm-offset-1">
    <legend>Personas que Participan</legend>

    <div class="col-sm-10 col-sm-offset-1">
        <table class="table table-striped table-bordered table-hover">
            <thead>
                <tr>
                    <th>Nº</th>
                    <th>Apellido y Nombre</th>
                    <th>Participa?</th>
                </tr>
            </thead>
            <tbody>
                {% for persona in form.participantes %}
                <tr>
                    <td>{{ loop.index }}</td>
                    <td>{{ form_label(persona) }}</td>
                    <td>{{ form_widget(persona) }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    <div class="pull-right">
        <div class="lead"><em>3</em> Participantes</div>
    </div>
</div>